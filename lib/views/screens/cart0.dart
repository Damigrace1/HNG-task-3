import 'package:flutter/cupertino.dart';

import '../../app_scaffold.dart';

class CartScreen0 extends StatelessWidget {
  const CartScreen0({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AppScaffold(
        title: "Cart",
        body: Column(children: [
          SizedBox(height: 200,),
          Text('Designer didnt implement this page')
        ],));
  }
}
